# VoiceTool ポータブル版 仕様書

## 概要
VoiceTool は、リファレンス音声に基づき感情豊かな音声を合成し、さらに速度やピッチの調整、波形編集が可能なツールです。

## 構成
配布フォルダ内には以下のファイルが含まれます：
- `VoiceTool.exe`: アプリケーション実行ファイル（`dist/VoiceTool/VoiceTool.exe`）
- `bin/`: 外部バイナリ（FFmpeg, Rubberband）が含まれるフォルダ。
- `models/`: 音声合成に使用するAIモデルが格納されるフォルダ。
- `projects/`: ユーザーのプロジェクトファイルが保存される場所。
- `docs/`: 本仕様書などのドキュメント。

## 動作環境
- OS: Windows 10/11 (64bit)
- GPU: NVIDIA GPU (VRAM 4GB以上推奨) ※CUDAによる高速化に対応
- ディスク空容量: AIモデルを含め数GB程度が必要となります。

## 使用方法
1. `VoiceTool.exe` を実行します。
2. 初回起動時に必要なモデルのダウンロードが行われる場合があります（インターネット接続が必要）。
3. 「テキスト」パネルにセリフを入力します。
4. 「リファレンス音声」をドロップまたは参照から設定します。
5. 「生成」ボタンで音声を合成します。
6. 「パラメータ」パネルで微調整を行い、再度生成することで波形が更新されます。
7. 「書き出し」ボタンで最終的な音声ファイルを保存できます。

## ライセンス
- ソフトウェア本体: MIT License
- 音声合成エンジン: Coqui TTS (利用規約に準じます)
- 学習済みモデル: 各モデルのライセンスに準じます。

---
2026-04-21 作成
