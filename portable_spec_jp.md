# VoiceTool v1.2.0 ポータブル版 仕様書

## 概要
VoiceTool は、リファレンス音声に基づき感情豊かな音声を合成し、さらに速度やピッチの調整、波形編集が可能なツールです。

## 構成
配布フォルダ内には以下のファイルが含まれます：
- `run.bat`: アプリケーション起動用バッチファイル（Python環境を自動検知）
- `build_native.bat`: 高速描画エンジンをビルドするためのスクリプト
- `bin/`: 外部バイナリ（FFmpeg, Rubberband）
- `models/`: 音声合成に使用するAIモデル
- `projects/`: ユーザープロジェクトの保存先
- `src/`: アプリケーションソースコード（開発・ビルド用）
- `docs/`: 技術仕様書および本マニュアル

## セットアップと高速化
### 1. 初回起動
`run.bat` を実行してください。必要なライブラリやAIモデルが不足している場合、自動的にセットアップが開始されます。

### 2. 高速モードの有効化（推奨）
波形描画を劇的に高速化するために、以下の手順でネイティブエンジンをビルドすることを推奨します：
1. `build_native.bat` を実行します。
2. 完了すると `src/voicetool/core/fast_waveform.dll` が生成されます。
3. 次回起動時から、SIMD（AVX2/SSE2）による超高速な波形描画が有効になります。

## 使用方法
1. `run.bat` を実行します。
2. 「テキスト」パネルにセリフを入力（1行が1セグメントになります）。
3. 使用したい声の「リファレンス音声」を設定します。
4. 「生成」ボタンで音声を合成（GPUがある場合は自動的に使用されます）。
5. タイムラインに表示された波形を確認し、感情やピッチ、速度を調整します。
6. ステレオ音声の読み込みやポーズ挿入も標準でサポートしています。
7. 「書き出し」で WAV/MP3 形式の最終ファイルを出力します。

## ライセンス
- ソフトウェア本体: MIT License
- 音声合成エンジン: Coqui TTS (利用規約に準じます)
- 学習済みモデル: 各モデルのライセンスに準じます。

---
2026-04-21 作成
