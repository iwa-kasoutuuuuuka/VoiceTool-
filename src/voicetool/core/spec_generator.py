# -*- coding: utf-8 -*-
"""
仕様書自動生成。起動時にVoiceTool_Spec.txtを生成する。
"""
import os
from datetime import datetime


def generate_spec(output_path: str):
    """
    VoiceTool_Spec.txtを生成する。
    
    Args:
        output_path: 出力先パス
    """
    spec = f"""================================================================================
  VoiceTool — 音声編集＋音声合成ツール 仕様書
  生成日時: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}
================================================================================

■ ソフト概要
  直感的でモダンなUIを持つ完全ローカル音声編集＋音声合成ツールです。
  テキストを入力し、セグメントごとに速さ・ピッチ・音量・感情などの
  パラメータを調整して自然な音声を生成できます。
  Coqui XTTS v2による高品質な音声クローニングに対応しています。

■ 機能一覧
  1. テキスト入力 & セグメント分割
     - 改行ごとに自動的にセグメントを分割
     - 各セグメントのパラメータを個別設定可能

  2. 音声合成 (Coqui XTTS v2)
     - ゼロショット音声クローニング
     - リファレンス音声 (WAV/MP3) による話者指定
     - 日本語・英語対応

  3. 音声加工
     - ピッチ変更 (rubberband使用、-12～+12半音)
     - 速度変更 (ffmpeg使用、0.5～2.0倍)
     - 音量調整 (-20～+20dB)
     - セグメント間ポーズ挿入

  4. 感情パラメータ
     - 幸せ / 楽しみ / 怒り / 悲しみ の4種類
     - 感情に応じてピッチ・速度・音量を自動調整
       pitch += happy*2 + angry*1 - sad*2
       speed += fun*0.3 - sad*0.2
       volume += angry*2

  5. タイムライン & 波形表示
     - 各セグメントの波形をビジュアル表示
     - セグメントの選択・編集
     - 再生カーソル表示

  6. 再生
     - セグメント単体再生
     - 全体通し再生
     - 停止機能

  7. 書き出し
     - WAV / MP3 形式で出力
     - 全セグメントを連結して書き出し
     - 安定したステレオチャンネル処理

  8. ネイティブ・アクセラレーション
     - C/Assembly (SIMD: AVX2/SSE2) による波形生成の高度な最適化
     - 数千万サンプルの解析を 1ms 以下で実行
     - DLL 欠如時の自動 NumPy フォールバック機能

  9. プロジェクト管理
     - .vproj形式（JSON）で保存・読込
     - 新規作成 / 開く / 保存

  10. キャッシュ & 安定性
      - 同一条件の音声は再生成せずキャッシュを利用
      - 数値的安定性保護（NaN / Inf ガード）
      - ステレオ音声のポーズ挿入サポート
      - ユニバーサル起動ロジック（python / py ランチャー自動検知）

■ 操作方法
  【基本操作】
  1. アプリケーションを起動する（初回はモデル自動ダウンロード）
  2. ツールバーの「リファレンス音声」横にサンプル音声をドロップ
  3. 左側のテキストエリアにテキストを入力（改行でセグメント分割）
  4. 右側のパラメータパネルで各種調整
  5. ツールバーの「再生」で試聴
  6. 「書き出し」で音声ファイルを出力

  【ショートカット】
  Ctrl+N  新規プロジェクト
  Ctrl+O  プロジェクトを開く
  Ctrl+S  プロジェクトを保存
  Ctrl+E  書き出し
  Space   再生/停止

■ フォルダ構成
  src/
    voicetool/
      main.py              メインエントリーポイント
      ui/                  UIモジュール
      core/                コアロジック
  models/                  XTTS v2モデル（自動DL）
  bin/                     ffmpeg / rubberband（自動DL）
  projects/                プロジェクト保存先
  temp/                    一時ファイル
  docs/                    仕様書・ドキュメント

■ 使用ライブラリ一覧
  - PyQt6              UIフレームワーク
  - coqui-tts          音声合成エンジン (XTTS v2)
  - torch              深層学習フレームワーク
  - numpy              数値計算
  - soundfile          音声ファイル読み書き
  - sounddevice        音声再生
  - pydub              音声加工ユーティリティ
  - ffmpeg             音声変換・速度変更
  - rubberband         ピッチ変更
  - ctypes             ネイティブ DLL 連携

■ トラブルシューティング
  Q: 起動時にモデルのダウンロードが失敗する
  A: インターネット接続を確認してください。3回リトライ後も失敗する場合、手動でmodelを配置してください。

  Q: 音声生成が遅い
  A: NVIDIA GPUとCUDA対応のPyTorchをインストールすることで大幅に高速化されます。

  Q: 音声が生成されない
  A: リファレンス音声が正しく設定されているか確認してください。ログパネルのエラーメッセージを確認してください。

  Q: GPUが認識されない
  A: NVIDIA GPUドライバとCUDA Toolkitが正しくインストールされているか確認してください。

  Q: プロジェクトファイルが開けない
  A: .vprojファイルが破損していないか確認してください。テキストエディタでJSONとして開けるか確認してください。

================================================================================
  ライセンス: MIT License
  ※ 使用している学習済みモデル(Coqui XTTS v2等)は各モデルのライセンスに従ってください。
================================================================================
"""
    os.makedirs(os.path.dirname(output_path) or ".", exist_ok=True)
    with open(output_path, "w", encoding="utf-8") as f:
        f.write(spec.strip())
